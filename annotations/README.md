# Source Annotations

这里保存从外部源码生成的教学性派生材料，不保存完整第三方 repository。

- 原始 checkout 继续位于被 `.gitignore` 忽略的 `sources/code/`。
- 每个项目按读取时的完整 commit SHA 分目录，避免覆盖旧版本。
- `annotated/` 只保留经过执行路径追踪后选出的核心摘录；它们不是原始源码的替代品。
- 入口 Skill：[source-code-annotation](../skills/source-code-annotation/SKILL.md)。
